import 'package:flutter/material.dart';

class FichePDipp extends StatelessWidget {
  const FichePDipp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
       child: Text('Fiche technique p-dipping')
    );
  }
}